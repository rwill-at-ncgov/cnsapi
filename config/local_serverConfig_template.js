/*
	Configure logging for app
*/

const port = 5008;
const serverIp = 'nn.nnn.nnn.nnn';
const serverHost = 'hostname';

const appPrefix = '/cnsapi';

const samlUrl = 'https://some_saml_url';

module.exports = {
	port: port,
	serverIp: serverIp,
	serverHost: serverHost,
	appPrefix: appPrefix,
	samlUrl: samlUrl
}

